CREATE TABLE user_minio (
                            id INT AUTO_INCREMENT PRIMARY KEY,
                            Name VARCHAR(50),
                            Age INT,
                            Email VARCHAR(50)
);